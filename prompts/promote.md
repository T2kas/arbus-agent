Today is {today}.

The following quick-mode market candidate has been SELECTED TO LAUNCH on
Arbus. Write its full-mode specification.

Use web search to:
- Re-verify the event is still live and undecided as of today.
- Confirm the primary resolution source exists and actually publishes the
  data the trigger sentence depends on.
- Find a genuinely independent backup source (different owner/organization
  than the primary).

The full-mode spec must contain:
- A precise trigger sentence in Lithuanian: exactly what observable fact, at
  what source, resolves the market "Taip" (or resolves each option).
- Definitions of every ambiguous term (what counts as "laimės", "pasirodys",
  "iki" — inclusive or exclusive, which timezone, official vs preliminary
  results, etc.).
- Named edge cases with explicit resolution for each (postponement,
  cancellation, tie, source silent, event partially happens...).
- Primary + independent backup source (URLs).
- Early-resolution / step-wise logic if the outcome can become certain before
  resolve_by, or "Netaikoma".
- A freeze-time hint: when trading should stop.

All user-facing text in Lithuanian. No gambling vocabulary.

CANDIDATE:
{candidate}
